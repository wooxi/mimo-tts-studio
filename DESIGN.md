# MiMo TTS Studio — Design Baseline

> 方向「瓷与朱」v3：浅色高级、简约克制的中文语音工坊。瓷白冷灰底 + 墨字 +
> 朱砂点睛（取自印章/录制红），波形是唯一视觉主角。
> v2 的「录音棚」暗色主题退役为「墨」深色变体（顶栏一键切换）。

## Atmosphere

一张安静的瓷面工作台。界面不是通用 Web 表单，也不是拟物 DAW——
是给中文配音/有声内容创作者的精密工具：留白、细线、克制的墨灰层级，
唯一的饱和色朱砂只出现在「值得被看见」的地方（合成按钮、播放进度、选中态）。

## Color Tokens（light 为默认，dark 为「墨」变体）

### Light（默认）

| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `oklch(0.978 0.004 250)` | 瓷白底（冷调，刻意避开奶油色） |
| `--surface` | `oklch(1 0 0)` | 面板/输入框 |
| `--surface-2` | `oklch(0.958 0.0045 250)` | 次级底/分段控件底 |
| `--surface-3` | `oklch(0.932 0.005 250)` | 进度条槽/更深层级 |
| `--border` | `oklch(0.908 0.007 250)` | 描边 |
| `--ink` | `oklch(0.24 0.012 250)` | 墨字 |
| `--ink-dim` | `oklch(0.44 0.013 250)` | 次级文字 |
| `--ink-faint` | `oklch(0.60 0.011 250)` | 辅助文字/未播放波形 |
| `--accent` | `oklch(0.545 0.185 27)` | 朱砂：合成按钮/分段墨块/播放进度 |
| `--accent-ink` | 近白 | 朱砂底上的文字 |
| `--accent-dim` | `… / 0.10` | 选中底/chips |
| `--ok` | `oklch(0.52 0.105 155)` | 成功（苔绿） |
| `--err` | `oklch(0.52 0.19 27)` | 错误（与朱砂同族，靠上下文区分） |

### Dark「墨」

`--bg oklch(0.168 0.008 250)` 近黑冷灰，朱砂提亮为 `oklch(0.675 0.165 27)`，
朱砂底上的文字用深墨。其余 token 等比映射。

## Typography

| 角色 | 字体 | 字号 |
| --- | --- | --- |
| 品牌字 | Georgia / 宋体（`--font-display`） | 0.98rem 600 |
| UI 正文 | system-ui 栈 | 0.875rem / line 1.6 |
| 合成文本输入 | system-ui 栈 | 0.94rem / line 1.8 |
| 读数（时间码/KB/字数/计数） | `'SF Mono','JetBrains Mono',Consolas` | 0.72–0.75rem，tabular-nums |

不引入外部字体文件。等宽只用于数字读数，衬线只用于品牌字，正文一律系统黑体。

## Layout

- 桌面双栏：顶栏 56px（品牌 · 同步状态 · 主题 · 设置）／
  左创作区 1.08fr（模型分段+描述 · 模型参数卡 · 合成文本+字数 · chips ·
  三个折叠区：风格标签/风格描述/导演模式）／右输出区 0.92fr
  （最近合成 N 条 · 播放器卡 · 历史列表）
- 模型切换放创作区顶部（参数卡片随模型变化的因果一目了然），带滑动墨块
- 合成按钮吸底（sticky + 渐隐底），始终可达
- 移动端（≤820px）：单列创作区；输出区为底部抽屉（54px 把手，上滑 76dvh），
  合成按钮吸底于把手之上，合成完成后自动展开抽屉
- 断点：1100px（右栏收窄、间距收紧）/ 820px（单列+抽屉）/ 480px（收纳同步文字、
  标签搜索独立成行、抽屉全宽）

## Primitives

- **分段 segmented**：瓷面凹槽 + 朱砂墨块（模型切换带滑动动效；格式/标签分组
  同语言静态版）
- **折叠 fold**：标题行 + 圆角 chevron 旋转，`grid-template-rows 0fr→1fr` 展开
- **chip**：朱砂 10% 底 + 朱砂字，圆角 999px，× 移除，弹入动效
- **tag pill**：风格实底浅灰、音效虚线描边（区分「选中风格」与「插入音效词」），
  选中转朱砂 10% 底
- **播放器卡**：白面板 + 64px 波形 canvas（未播放墨灰、已播放朱砂，双色填充
  表达进度）+ 圆形播放钮 + 3px 进度条（hover 显拖点）+ mono 读数
- **历史条目**：白卡一行文本 + mono 时间，meta 行（音色/时长/KB），hover 浮现
  载入/删除；播放中转朱砂描边；触屏设备操作恒显
- **抽屉 drawer**：右侧 400px，遮罩渐显，Esc 可关

## Motion

回答操作的动效：分段墨块滑动 200ms、折叠 grid-rows 200ms、chip 弹入
（回弹曲线）、抽屉 280ms、历史删除淡出位移、播放进度墨色填充。
页面加载只有一次整幅 `settle`（顶栏+双栏 10px 上浮渐显）。
历史列表一次性错峰入场（≤12 项，30ms 步进）。
`prefers-reduced-motion` 全部归零。同步状态点呼吸动效仅用于进行中。

## Voice / 文案

界面文案中文为主，术语保留英文（WAV、PCM16、VoiceDesign、VoiceClone）。
按钮说动作（合成/下载/载入），空态一句话指路，不放插图堆砌。
图标全部为手绘线性 SVG，不用 emoji。

## Source Evidence & Confidence

- v2 `public/`（录音棚暗色版）为先行来源，本次为有意的整体替换式重设计
- `[inferred confidence=high]` 全部 token 值为本设计权威新定
- 功能字段保持兼容：MODELS/PRESET_VOICES/STYLE_TAGS/AUDIO_TAGS、
  localStorage key（`mimo_tts_config` / `mimo_tts_theme` /
  `mimo_tts_config_token`）及 KV 配置字段名不变
